import { useEffect } from 'react';
import type { PmsLogValue } from '../types';
import { formatDate } from '../lib/date';
export function LogSheet({date,value,onClose,onSave}:{date:string;value?:PmsLogValue;onClose:()=>void;onSave:(v?:PmsLogValue)=>void}){useEffect(()=>{const previous=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{document.body.style.overflow=previous}},[]);return <div className="sheet-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><section className="sheet" role="dialog" aria-modal="true" aria-labelledby="sheet-title"><div className="sheet-handle"/><button className="sheet-close" onClick={onClose}>×</button><p className="eyebrow">Daily entry</p><h2 id="sheet-title">{formatDate(date,{weekday:'long',day:'numeric',month:'long',year:'numeric'})}</h2><p>Did you experience PMS-like symptoms this day?</p><div className="sheet-actions"><button className={value==='pms'?'selected pms-button':''} onClick={()=>onSave('pms')}>Yes, PMS-like symptoms</button><button className={value==='no_pms'?'selected no-button':''} onClick={()=>onSave('no_pms')}>No symptoms</button>{value&&<button className="remove-button" onClick={()=>onSave(undefined)}>Remove manual entry</button>}</div></section></div>}
